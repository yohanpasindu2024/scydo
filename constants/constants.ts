export enum MarkerName {
    SOFTA = 0,
    GIXIN= 1,
}

export enum PenAmount {
    TWO = 0,
    SIX = 1,
    TWELVE = 2,
}

export enum Colors {
    BLUE = 0,
    RED = 1,
    BLACK = 2,
    GREEN = 3,
}